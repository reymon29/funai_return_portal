<%= content_for(:title) do %>
Funai - Aaron's
<% end %>
<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <h1>RMA Log</h1>
    </div>
      <div class="col-md-8 col-md-offset-3">
      <% @returns.each do |return_item| %>
        <h4><strong><%= return_item.item_number %> <%= return_item.product.model_number %>  <%= return_item.rma_status %> <%= link_to "Open", return_path(return_item) %></strong></h4></br>
      <% end %>
    </div>
  </div>
</div>

  <%= link_to "Dashboard", dashboard_path, class: "btn btn-primary" %>

